public class PlayerUndoManager : UndoManager<Character>
{
    public static PlayerUndoManager Instance { get; private set; }

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
